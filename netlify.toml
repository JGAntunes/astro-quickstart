# example netlify.toml
[build]
  command = "ls -la ./node_modules/cypress/node_modules && ls ./node_modules/cypress/node_modules/log_symbols/node_modules && cypress install && astro build"
  functions = "netlify/functions"
  publish = "dist"

# [[plugins]] 
#   package = "netlify-plugin-cypress"

#   [plugins.inputs.postBuild]
#     enable = true
  
#   [plugin.inputs]
#     enable = false
